{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Rust (output folder)",
            "type": "shell",
            "command": "mkdir -p output && rustc ${file} -o output/${fileBasenameNoExtension}",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
